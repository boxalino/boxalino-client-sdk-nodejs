import { SlaveSystem } from "./SlaveSystem";
import { IServerConnector } from "../../protocol/communicator/IServerConnector";
export interface ISlaveClient extends SlaveSystem {
    connect(ip: string, port: number): void;
}
export declare abstract class SlaveClient extends SlaveSystem implements ISlaveClient {
    /**
     * Default Constructor.
     */
    constructor();
    /**
     * @inheritdoc
     */
    protected abstract createServerConnector(): IServerConnector;
    /**
     * @inheritdoc
     */
    connect(ip: string, port: number): void;
}
