import { ICaseGenerator } from "./ICaseGenerator";
/**
 * A permutation case generator.
 *
 * <sub>n</sub>P<sub>r</sub>
 *
 * @author Jeongho Nam <http://samchon.org>
 */
export declare class Permutation implements ICaseGenerator {
    /**
     * @hidden
     */
    private size_;
    /**
     * @hidden
     */
    private n_;
    /**
     * @hidden
     */
    private r_;
    /**
     * Construct from size of N and R.
     *
     * @param n Size of candidates.
     * @param r Size of elements of each case.
     */
    constructor(n: number, r: number);
    /**
     * @inheritdoc
     */
    size(): number;
    /**
     * Get N, number of candidates.
     */
    n(): number;
    /**
     * Get R, number of elements for each case.
     */
    r(): number;
    /**
     * @inheritdoc
     */
    at(index: number): number[];
}
