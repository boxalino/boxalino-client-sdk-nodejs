import * as std from "tstl";
import { XML } from "sxml";
import { IEntity } from "./IEntity";
/**
 * @inheritdoc
 */
export declare abstract class EntityArray<T extends IEntity> extends std.Vector<T> {
    /**
     * @inheritdoc
     */
    construct(xml: XML): void;
    /**
     * @inheritdoc
     */
    abstract createChild(xml: XML): T;
    /**
     * @inheritdoc
     */
    key(): any;
    /**
     * @inheritdoc
     */
    has(key: any): boolean;
    /**
     * @inheritdoc
     */
    count(key: any): number;
    /**
     * @inheritdoc
     */
    get(key: any): T;
    /**
     * @inheritdoc
     */
    abstract TAG(): string;
    /**
     * @inheritdoc
     */
    abstract CHILD_TAG(): string;
    /**
     * @inheritdoc
     */
    toXML(): XML;
}
/**
 * @inheritdoc
 */
export declare abstract class EntityList<T extends IEntity> extends std.List<T> {
    /**
     * @inheritdoc
     */
    construct(xml: XML): void;
    /**
     * @inheritdoc
     */
    abstract createChild(xml: XML): T;
    /**
     * @inheritdoc
     */
    key(): any;
    /**
     * @inheritdoc
     */
    has(key: any): boolean;
    /**
     * @inheritdoc
     */
    count(key: any): number;
    /**
     * @inheritdoc
     */
    get(key: any): T;
    /**
     * @inheritdoc
     */
    abstract TAG(): string;
    /**
     * @inheritdoc
     */
    abstract CHILD_TAG(): string;
    /**
     * @inheritdoc
     */
    toXML(): XML;
}
/**
 * @inheritdoc
 */
export declare abstract class EntityDeque<T extends IEntity> extends std.Deque<T> {
    /**
     * @inheritdoc
     */
    construct(xml: XML): void;
    /**
     * @inheritdoc
     */
    abstract createChild(xml: XML): T;
    /**
     * @inheritdoc
     */
    key(): any;
    /**
     * @inheritdoc
     */
    has(key: any): boolean;
    /**
     * @inheritdoc
     */
    count(key: any): number;
    /**
     * @inheritdoc
     */
    get(key: any): T;
    /**
     * @inheritdoc
     */
    abstract TAG(): string;
    /**
     * @inheritdoc
     */
    abstract CHILD_TAG(): string;
    /**
     * @inheritdoc
     */
    toXML(): XML;
}
